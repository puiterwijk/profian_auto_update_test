apiVersion: apps/v1
kind: Deployment
metadata:
  name: steward
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: steward
        image: ghcr.io/profianinc/steward:0.2.0
        # main is a rolling branch, make sure to always re-pull
        imagePullPolicy: Always
